        // why type script matters?

        // TypeScript matters for several reasons, especially in the context of modern web development. Here are some key points highlighting its importance:

        // 1. **Static Typing**: TypeScript introduces static typing to JavaScript, allowing developers to define types for variables, function parameters, and return values. This helps catch errors at compile time rather than at runtime, leading to more robust and maintainable code.

        // 2. **Improved Developer Experience**: With features like autocompletion, type inference, and better tooling support (e.g., in IDEs), TypeScript enhances the developer experience. It makes it easier to navigate codebases and understand how different parts of the application interact.

        // 3. **Enhanced Code Quality**: By enforcing type checks and providing interfaces and type aliases, TypeScript encourages developers to write cleaner and more structured code. This can lead to fewer bugs and a more predictable codebase.

        // 4. **Scalability**: As applications grow in size and complexity, TypeScript's type system helps manage that complexity. It allows for better organization of code, making it easier to maintain and scale large projects.