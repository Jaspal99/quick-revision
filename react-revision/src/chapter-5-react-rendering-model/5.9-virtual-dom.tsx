// defination: A virtual DOM is a lightweight representation of the actual DOM in memory. React uses the virtual DOM to optimize updates and minimize direct manipulation of the actual DOM, which can be slow and resource-intensive.

// Reacts declarative model allows developers to describe the desired UI state, and React takes care of efficiently updating the actual DOM to match that state. The virtual DOM acts as an intermediary between the application state and the actual DOM, enabling React to perform efficient diffing and reconciliation.

// performace still depends on component and bundle size
